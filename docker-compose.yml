services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: mcp_control
      POSTGRES_USER: mcp
      POSTGRES_PASSWORD: mcp_secret
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
